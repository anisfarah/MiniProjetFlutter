import 'package:flutter/material.dart';

class ComposantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Liste Composants'),
      centerTitle: true,
      backgroundColor: Colors.deepPurpleAccent,
    ),
  );
}